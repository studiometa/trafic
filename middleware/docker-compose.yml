services:
  middleware:
    build: .
    container_name: traffic-middleware
    environment:
      - WEB_DOCUMENT_ROOT=/app/public
      - REDIS_HOST=redis
    networks:
      - traffic-network
      - ddev_default
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - .:/app
    restart: always
    working_dir: /app

  queue:
    image: webdevops/php:8.3
    container_name: traffic-queue
    environment:
      - WEB_DOCUMENT_ROOT=/app/public
      - REDIS_HOST=redis
    networks:
      - traffic-network
    volumes:
      - .:/app
    restart: always
    working_dir: /app
    command: ["php", "/app/bin/console", "messenger:consume", "--all", "-vvv"]

  redis:
    image: redis:7.4.2
    container_name: traffic-redis
    networks:
      - traffic-network
    ports:
      - "6379:6379"
    volumes:
      - traffic-redis-data:/data

volumes:
  traffic-redis-data: {}

networks:
  traffic-network:
    driver: bridge
  ddev_default:
    external: true
    name: ddev_default
